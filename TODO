Change all DB string limits to 255 or more (where it makes sense)

Make all foreign keys checked where appropriate and return 404 as a result

Make existsAllSecondary() for ManyDefined and check that as well (use count inside the check?)
