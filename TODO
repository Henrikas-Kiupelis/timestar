Make account creation process make more sense
    Do something like generate a token that can be used to access a page, where a password could be entered
        This should be discussed

Use commons classes from latest version in Lesson V3

Remove partitionId from calls until it is required (i.e. new PartitionAccount() in repository) for Lesson v3

Over time, fix all deprecation warnings
