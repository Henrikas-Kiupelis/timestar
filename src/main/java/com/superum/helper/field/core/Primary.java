package com.superum.helper.field.core;

/**
 * <pre>
 * A boolean replacement for defining if a field is primary key or not
 *
 * This was chosen to simplify the instantiation signatures for fields
 * (using true/false is too obfuscating in this case)
 * </pre>
 */
public enum Primary {

    NO, YES

}
